Implement task from plan.

Task: [TASK_FULL_TEXT]
Context: [CONTEXT]

If unclear: ask now.
Job: implement exactly what spec says, write tests (red-green-refactor), verify, self-review, report.

Self-review: completeness (all requirements?), quality (clean code?), discipline (YAGNI?), testing (real behavior verification?).

Report: DONE|DONE_WITH_CONCERNS|BLOCKED|NEEDS_CONTEXT + what implemented + test results + files changed + self-review findings.
