{
  "name": "agnes",
  "version": "0.10.1",
  "description": "AGNES — OpenCode Native Plugin: orchestrator plugin that routes tasks across 23 fused skills",
  "type": "module",
  "main": ".opencode/plugins/agnes.js",
  "repository": "github:nathwn12/agnes",
  "keywords": ["opencode", "plugin", "agent", "orchestrator", "skills"],
  "license": "MIT",
  "scripts": {
    "bundle": "bun build src/plugin.ts --outfile .opencode/plugins/agnes.js --target bun",
    "bundle:watch": "bun build src/plugin.ts --outfile .opencode/plugins/agnes.js --target bun --watch",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.15.5",
    "@types/node": "^25.9.1",
    "typescript": "^5.7"
  }
}
